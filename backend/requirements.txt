fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
faster-whisper==1.1.1
requests==2.32.3
huggingface-hub==0.27.1
anthropic==0.120.2

# Persistance et comptes
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.5.0

# Diarisation des locuteurs (torch CPU installé séparément dans le Dockerfile
# via l'index dédié, pour éviter de tirer les paquets CUDA volumineux)
pyannote.audio==3.3.2

# Export des documents
python-docx==1.2.0
openpyxl==3.1.5

# Analyse quantitative (alpha de Cronbach, corrélations avec significativité)
numpy==2.5.1
scipy==1.18.0

# Vérification des achats Google Play Billing (API Android Publisher)
google-auth==2.56.3

# Graphiques du rapport d'analyse quantitative
matplotlib==3.11.1

# Analyses statistiques avancées (AFE, AFC, regression, mediation)
factor_analyzer==0.5.1
# scikit-learn < 1.6 : factor_analyzer 0.5.1 utilise un paramètre supprimé au-delà (force_all_finite)
scikit-learn==1.5.2
semopy==2.3.11
statsmodels==0.14.6
